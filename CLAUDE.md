# Shared Team Agent Instructions

These instructions are version-controlled and shared across the team. DevEasy
injects this file (and `.claude/skills` + `.claude/agents`) into whichever project
you open, so everyone runs the same workflow. Edit it through the **Agent Manager**
in DevEasy, not by hand — saving commits the change so teammates get it on pull.

## Conventions

- (Add your team's shared coding and workflow conventions here.)
